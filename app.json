{
    "scripts": {
        "postinstall": "npm install && npm run build"
    },
    "environments": {
        "test": {
            "scripts": {
                "test-setup": [
                    "composer install",
                    "npm install",
                    "npm run build"
                ]
            }
        }
    }
}
